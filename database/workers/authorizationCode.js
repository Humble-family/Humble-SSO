const pool = require('./pool');
const queries = require('./queries/authorizationCode');

const getAuthorizationCode = ac => {

};

const saveAuthorizationCode = ac => {

};

module.exports = {
  getAuthorizationCode, saveAuthorizationCode
};